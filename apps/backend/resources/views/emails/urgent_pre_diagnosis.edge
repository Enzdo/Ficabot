<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ subject }}</title>
  <style>
    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
      line-height: 1.6;
      color: #374151;
      background-color: #F9FAFB;
      margin: 0;
      padding: 0;
    }
    .container {
      max-width: 600px;
      margin: 40px auto;
      background: white;
      border-radius: 12px;
      overflow: hidden;
      box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
    }
    .header {
      background: linear-gradient(135deg, {{ urgencyColor }} 0%, {{ urgencyColor }}dd 100%);
      color: white;
      padding: 32px 24px;
      text-align: center;
    }
    .header h1 {
      margin: 0 0 8px 0;
      font-size: 24px;
      font-weight: 700;
    }
    .urgency-badge {
      display: inline-block;
      background: rgba(255, 255, 255, 0.2);
      padding: 6px 16px;
      border-radius: 999px;
      font-size: 12px;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }
    .content {
      padding: 32px 24px;
    }
    .info-section {
      background: #F3F4F6;
      padding: 20px;
      border-radius: 8px;
      margin: 24px 0;
    }
    .info-row {
      display: flex;
      margin-bottom: 12px;
    }
    .info-row:last-child {
      margin-bottom: 0;
    }
    .info-label {
      font-weight: 600;
      color: #6B7280;
      min-width: 120px;
    }
    .info-value {
      color: #1F2937;
    }
    .description-box {
      background: #FEF3C7;
      border-left: 4px solid #F59E0B;
      padding: 16px;
      border-radius: 4px;
      margin: 24px 0;
    }
    .description-box h3 {
      margin: 0 0 8px 0;
      color: #92400E;
      font-size: 14px;
      font-weight: 600;
      text-transform: uppercase;
    }
    .description-box p {
      margin: 0;
      color: #78350F;
      line-height: 1.5;
    }
    .cta-button {
      display: inline-block;
      background: {{ urgencyColor }};
      color: white;
      text-decoration: none;
      padding: 14px 32px;
      border-radius: 8px;
      font-weight: 600;
      text-align: center;
      margin: 24px 0;
      transition: opacity 0.2s;
    }
    .cta-button:hover {
      opacity: 0.9;
    }
    .footer {
      background: #F9FAFB;
      padding: 24px;
      text-align: center;
      font-size: 13px;
      color: #6B7280;
      border-top: 1px solid #E5E7EB;
    }
    .footer a {
      color: {{ urgencyColor }};
      text-decoration: none;
    }
  </style>
</head>
<body>
  <div class="container">
    <!-- Header -->
    <div class="header">
      <h1>🚨 Pré-diagnostic urgent</h1>
      <span class="urgency-badge">Urgence: {{ urgencyLabel }}</span>
    </div>

    <!-- Content -->
    <div class="content">
      <p>Bonjour Dr. {{ veterinarian.name }},</p>

      <p>Un nouveau pré-diagnostic urgent nécessite votre attention immédiate sur la plateforme Ficabot.</p>

      <!-- Patient Info -->
      <div class="info-section">
        <h3 style="margin-top: 0; color: #1F2937; font-size: 16px;">Informations sur l'animal</h3>
        <div class="info-row">
          <span class="info-label">Nom:</span>
          <span class="info-value">{{ pet.name }}</span>
        </div>
        <div class="info-row">
          <span class="info-label">Espèce:</span>
          <span class="info-value">{{ pet.species === 'dog' ? 'Chien' : 'Chat' }}</span>
        </div>
        @if(pet.breed)
        <div class="info-row">
          <span class="info-label">Race:</span>
          <span class="info-value">{{ pet.breed }}</span>
        </div>
        @endif
        @if(pet.age)
        <div class="info-row">
          <span class="info-label">Âge:</span>
          <span class="info-value">{{ pet.age }} an(s)</span>
        </div>
        @endif
      </div>

      <!-- Symptoms Description -->
      <div class="description-box">
        <h3>Description des symptômes</h3>
        <p>{{ preDiagnosis.userDescription }}</p>
      </div>

      <!-- CTA Button -->
      <div style="text-align: center;">
        <a href="{{ dashboardUrl }}/dashboard/pre-diagnoses/{{ preDiagnosis.id }}" class="cta-button">
          Voir le pré-diagnostic complet →
        </a>
      </div>

      <p style="margin-top: 32px; color: #6B7280; font-size: 14px;">
        Votre expertise est précieuse pour le bien-être de cet animal. Merci de votre réactivité.
      </p>
    </div>

    <!-- Footer -->
    <div class="footer">
      <p>
        Cet email a été envoyé par <strong>Ficabot</strong><br>
        Plateforme de santé vétérinaire assistée par IA
      </p>
      <p style="margin-top: 12px;">
        <a href="{{ dashboardUrl }}">Accéder au tableau de bord</a> •
        <a href="{{ dashboardUrl }}/settings">Gérer les notifications</a>
      </p>
    </div>
  </div>
</body>
</html>
